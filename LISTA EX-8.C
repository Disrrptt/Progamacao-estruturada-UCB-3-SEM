#include <stdio.h>
#include <stdlib.h>

int main() {
    int valorA, valorB, valorC, valorD, diferenca;
    
        scanf("%d %d %d %d", &valorA, &valorA, &valorC, &valorD);
        diferenca = (valorA * valorB) - (valorC * valorD);
    
        printf("DIFERENCA = %d * %d - %d * %d = %d\n", valorA, valorB, valorC, valorD, diferenca);
        return 0;
}
